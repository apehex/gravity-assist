[[source]]
verify_ssl = true
name = "pypi"
url = "https://pypi.python.org/simple"

[requires]
python_version = "3.5"

[packages]
bumpversion = ">=0.5.3"
click = ">=6.0"
coverage = ">=4.1"
"flake8" = ">=2.6.0"
pytest = ">=2.9.2"
pytest-runner = ">=2.11.1"
Sphinx = ">=1.4.8"
tox = ">=2.3.1"
watchdog = ">=0.8.3"

[dev-packages]
